# Performance option visibility fix

The Performance control remains available in both Dashboard controls and Engine Settings.
The Engine Settings version is made immediately visible as a dedicated section near the top of the modal, while the Rendering section remains focused on text clarity.

Safety boundary:
- No performance state semantics changed.
- `high` and `ultra` remain the only two values.
- No glass-effect CSS behavior is changed by this visibility fix.
